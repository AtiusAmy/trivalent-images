type: dnf
repos:
  cleanup: true
  files:
    - https://repo.secureblue.dev/secureblue.repo
  copr:
    - name: secureblue/trivalent
      chroot: fedora-43-x86_64
install:
  packages:
    - trivalent
    - trivalent-subresource-filter
